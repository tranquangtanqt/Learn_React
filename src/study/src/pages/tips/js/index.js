import { Divider} from "antd";
import MasterLayout from "../../../themes/masterLayout";

const TrickJS = () => {

  return (
    <MasterLayout>
       <Divider orientation="left">Input</Divider>
    </MasterLayout>
  );
};

export default TrickJS;
