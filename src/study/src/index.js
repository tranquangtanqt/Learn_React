import React from "react";
import ReactDOM from "react-dom/client";
import RoutesRoot from "./pages/route";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <RoutesRoot />
  </React.StrictMode>
);
